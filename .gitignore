.env
venv/
__pycache__/
*.pyc
.DS_Store
*.db
logs/
.streamlit/secrets.toml
emergency_stop.flag
saas_emergency_stop.flag

# Runtime state files -- only ever exist on the droplet, never meant to
# be part of source control.
etoro_position_state.json
alpaca_highest_profit_state.json
backups/

# Private strategy config -- see config.example.py's header comment for why.
config.py

# Trained model artifacts -- large binary, deployed separately, not
# meant to bloat the repo.
models/

# Stray SQLite journal files from local testing.
*.db-journal

# Backtest engine's local historical-data cache and run outputs -- large,
# derived, and machine-specific; never meant to be committed.
backtest_cache/
backtest_results/

# tune_backtest.py's parameter-sweep output -- same reasoning as
# backtest_results/ above.
tuning_results/
